# ── Neon (Data DB) ─────────────────────────
POSTGRES_URL=
POSTGRES_HOST=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# ── Airflow Connections ─────────────────────
# Auto-registers pg_conn in Airflow — no manual UI setup needed
# Same value as POSTGRES_URL
AIRFLOW_CONN_PG_CONN=

# ── Airflow Webserver ───────────────────────
# Generate a strong secret key:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW_SECRET_KEY=changeme-in-production